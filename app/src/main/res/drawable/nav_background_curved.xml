<!-- bottom_nav_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer circle (black border) -->
    <stroke
        android:width="1dp"
    android:color="#CDECECEC" /> <!-- border color -->
    <solid android:color="#CDECECEC" /> <!-- background color -->
    <corners android:radius="20dp"/>
</shape>
